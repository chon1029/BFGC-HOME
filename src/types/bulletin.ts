export interface Bulletin {
    _id: string
    title: string
    date: string
    volume?: string
    thumbnail?: string // URL string for mock
    pdfFile?: string // URL string for mock
    sermonTitle?: string
    preacher?: string
    scripture?: string
}
